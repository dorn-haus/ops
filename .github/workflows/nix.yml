name: Nix

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  check:
    runs-on: namespace-profile-ubuntu-nix

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check Nix flake
        run: nix flake check
